# READ BEFORE DOING ANYTHING
For some reason, the stm32-for-vscode extension doesn't want to work however,
we can use the `make` tools directly to build, and clean.

**Flashing** using the extension does seem to work.